# 红色文化站 · 整改处理计划（含完整修改建议与执行顺序）

> 基于 `website-review.md` 评审结论整理。目标：把"个人精品"收口为"可长期维护的生产级站点"。
> 排序原则：**先灭火（阻断退步/显性风险）→ 再收口结构（最大短板）→ 后提性能/补测试 → 最后打磨细节**。
> 每个条目含：现状与风险、具体修改动作、涉及文件、依赖、验证方式、预估工作量。

---

## 〇、执行顺序总览（依赖链）

```
Phase 0  灭火（本周·立即）          无前置依赖，独立可并行
  ├─ 0.1  生成器加禁用注释/移除
  ├─ 0.2  备案续费启动（环境 9-19 到期 → 硬截止）
  └─ 0.3  排行榜孤儿：决策 + 挂页面 或 暂时软屏蔽云函数

Phase 1  结构收口（本月）            依赖 0.1 生效（先拆弹再动结构）
  ├─ 1.1  统一脚本加载路径（去 cloud-lazy VER 旁路）
  ├─ 1.2  加根 package.json + 部署/版本脚本
  └─ 1.3  敏感词库合并为单一来源

Phase 2  性能与健壮性（本月～下月）
  ├─ 2.1  自托管字体（去外部 CDN 依赖）
  ├─ 2.2  corpus 分片懒加载
  └─ 2.3  补 ai-chat 单测（复用已有 _test 钩子）

Phase 3  安全头与细节（长期·按需）
  ├─ 3.1  CSP 响应头（★依赖先改内联 <style>）
  ├─ 3.2  authMode 命名修正 / 注释
  └─ 3.3  视觉微调（页脚/时间轴，主观可选）
```

**为什么是这个顺序**：0.1/0.2 是"不处理就会继续恶化或到期"的硬约束；0.3 是审计遗留⑤长期未闭环。Phase 1 的"双加载路径"是最大可维护性短板，但改动面大，**必须等 0.1 的生成器炸弹先拆除**，否则结构重组时极易误触生成器回退。Phase 3 的 CSP 必须最后做，因为它要求先消除内联 `<style>`（否则 `unsafe-inline` 会让 CSP 形同虚设）。

---

## Phase 0 — 灭火（本周·立即执行）

### 0.1 生成器 `scripts/generate-*.mjs`：加禁用注释（或移除）
- **现状/风险**：3 个 `generate-*.mjs` 仍写死被墙的 `fonts.googleapis.com`/`fonts.gstatic.com`，与已部署页（已换 `fonts.loli.net`）脱节。任何人重跑即批量生成白屏页——定时炸弹。
- **修改建议（三选一，强烈建议 ① 或 ②）**：
  1. **（推荐）文件头加醒目禁用注释**：`/* ⚠ 禁止运行：与部署页脱节，运行即生成白屏页。改单页请手工编辑，勿用本生成器。*/`，并在 `.gitignore` 不忽略（保留为归档）。
  2. **直接移除生成器**：`git rm scripts/generate-*.mjs`，改由手工编辑单页（与现有工作流一致，memory 已规定"永远不要运行 generate-*.mjs"）。
  3. （不推荐）同步生成器到 `loli.net` + 当前 CSS 类名与 `?v=`——维护成本高且易再次脱节。
- **涉及文件**：`scripts/generate-*.mjs`（3 个）；可选 `.gitignore`。
- **依赖**：无。
- **验证**：`grep -rl "googleapis" scripts/` 确认已无被墙域名（选 ② 则脚本不存在）。
- **工作量**：S（约 5 分钟）。

### 0.2 备案续费启动（硬截止 2026-09-19）
- **现状/风险**：CloudBase 环境 **2026-09-19 到期**；站内备案要求剩余有效期 > 6 个月，且需开云托管固定 IP。环境一旦到期，整站下线。
- **修改建议**：
  1. 立即续费环境（路径 A），开云托管固定 IP。
  2. 续费后启动 ICP 备案（腾讯云接入）→ 公安备案 → 绑自定义域名 + SSL。
  3. 拿到真实备案号后，用一条全局替换把全站 `ICP备案号：待填写` / `公网安备：待填写` 替换为真实号（52 个内容页 footer 已静态写入，不再只改 `script.js`）。
- **涉及文件**：全站 52 个内容页 footer（占位已就位，替换即可）。
- **依赖**：无（但阻塞一切备案动作）。
- **验证**：环境控制台确认到期日已顺延 > 6 个月；`grep -rn "待填写" *.html` 替换后为 0。
- **工作量**：M（需人工在云控制台操作 + 等待审核，非代码工作量）。

### 0.3 排行榜孤儿功能：闭环
- **现状/风险**：`quiz-rank.js` + `cloudfunctions/quiz-rank/index.js` 实现完整（含昵称敏感词过滤、隐私脱敏），但**全站无任何页面加载它**（无 `rank-modal`/`rank-open` 挂载点）。审计遗留⑤未闭环，已部署的"成绩"无公开展示出口。
- **修改建议（二选一）**：
  1. **（推荐）挂上页面**：在「我的成绩」面板（`account-ui.js` 已渲染）或独立 `/rank` 页引入 `quiz-rank.js`，加 `rank-open` 触发按钮 + `rank-modal` 弹窗（复用现有 dialog aria 规范：`role="dialog"` `aria-modal` `Esc` 关闭 `focus` 管理）。前端 `quiz-service.js` 的客户端昵称预检保留。
  2. **（临时）软屏蔽云函数**：若本月来不及做页面，先 `tcb fn delete quiz-rank` 或注释其触发，避免"部署了却看不到"的歧义；待页面就绪再部署。
- **涉及文件**：`quiz-rank.js`、`cloudfunctions/quiz-rank/`、`account-ui.js`（或新建 `rank.html`）。
- **依赖**：无（独立功能）。
- **验证**：真机登录 → 完成一次答题 → 打开排行榜 → 看到昵称/分数/时长；`grep -rn "quiz-rank.js" *.html` 命中 ≥1。
- **工作量**：M（挂页面约 0.5–1 天；软屏蔽约 5 分钟）。

---

## Phase 1 — 结构收口（本月·消除最大短板）

### 1.1 统一脚本加载路径（去 cloud-lazy VER 旁路）
- **现状/风险**：45 页用 `<script src="x.js?v=...">` 直接引；7 红文页走 `window.RCS_CLOUD_SCRIPTS` + `cloud-lazy.js` 顺序注入（注入时剥清单内 `?v=` 再套自身 `VER`）。改云脚本须同步**三处**（45 页 `?v=` + `cloud-lazy.js` 的 `VER` + 7 页的 `cloud-lazy.js?v=`），易漏改致云脚本陈旧。
- **修改建议**：
  1. **（推荐）让 7 红文页回退到直接 `?v=` 引入**，与 45 页统一，删除 `cloud-lazy.js` 的 `VER` 旁路（保留 `cloud-lazy.js` 仅做 SDK 注入）。
  2. 或抽 `deploy-version.js` 集中管版本号，`cloud-lazy.js` 与 45 页都从同一处读 `VER`。
- **涉及文件**：7 红文页（`RCS_CLOUD_SCRIPTS` 块）、`cloud-lazy.js`、`script.js`（注入逻辑）。
- **依赖**：0.1 已生效（先拆弹）。
- **验证**：改一处云脚本 → 改版本号 → 真机在 45 页与 7 红文页各验一次登录态/AI 角标一致（memory 铁律：认证导航改动后两类页真机各验）。
- **工作量**：M（约 0.5–1 天，需两类页各验）。

### 1.2 加根 package.json + 部署/版本脚本
- **现状/风险**：无根 `package.json`、无构建编排；部署靠手工 rsync + tcb；云函数依赖仅各函数 `package.json`，无统一锁文件，漏步风险高。
- **修改建议**：
  1. 根目录加 `package.json`，`scripts` 含：`deploy`（rsync 到 `/tmp/rcs-deploy` → `tcb hosting deploy`）、`bump`（统一升级 `?v=` 版本号）、`dev`（本地起静态服务 + tcb 模拟）。
  2. 云函数依赖提取统一锁文件（或 `npm workspaces`）。
- **涉及文件**：新建 `package.json`；`tcb` 部署命令固化进脚本。
- **依赖**：1.1 路径统一后版本号更好管（可并行）。
- **验证**：`npm run deploy` 一键上线；`npm run bump` 后全站 `?v=` 一致。
- **工作量**：M（约 0.5 天）。

### 1.3 敏感词库合并为单一来源
- **现状/风险**：`cloudfunctions/ai-chat/lib/sensitive-words.js` 与 `cloudfunctions/quiz-rank/lib/sensitive-words.js` 两份，新增词易只改一处。
- **修改建议**：
  1. 抽到 `cloudfunctions/_shared/sensitive-words.js`，两函数 `require('../_shared/sensitive-words.js')`。
  2. 或符号链接指向同一文件（部署时需确认 tcb 保留链接）。
- **涉及文件**：`cloudfunctions/ai-chat/lib/`、`cloudfunctions/quiz-rank/lib/`、`cloudfunctions/_shared/`（新建）。
- **依赖**：无。
- **验证**：两函数 `scanSensitive` 返回一致；部署后实测命中一次。
- **工作量**：S（约 15 分钟）。

---

## Phase 2 — 性能与健壮性（本月～下月）

### 2.1 自托管字体（去外部 CDN 依赖）
- **现状/风险**：`fonts.loli.net`（虽国内可达）仍属 render-blocking 外部依赖；无 SRI；单点风险。
- **修改建议**：
  1. 下载 `Noto Sans SC` woff2 自托管到 CloudBase 静态托管 `/fonts/`。
  2. `<link rel="preload" as="font" crossorigin>` + `@font-face` 指向本地；移除 `loli.net` 引用。
- **涉及文件**：`style.css`、`nav-embed.css`、`index.html` 等头部；新增加 `assets/fonts/`。
- **依赖**：无（独立）。
- **验证**：Lighthouse 字体请求数↓、首屏无外部字体阻塞；`grep -rn "loli.net" *.css *.html` 为 0。
- **工作量**：M（约 0.5 天，含字体子集化）。

### 2.2 corpus 分片懒加载
- **现状/风险**：`corpus.json` 537KB / 602 条，AI 首开即拉全量，浪费带宽。
- **修改建议**：按书目（红岩/红星/…）分片，首次只拉当前书目分片；或 IndexedDB 缓存 + 按需取。
- **涉及文件**：`data/corpus.json`、`ai-assistant.js`、`build_corpus.py`。
- **依赖**：无。
- **验证**：AI 首开网络面板 corpus 体积显著下降；答题/来源引用仍准确。
- **工作量**：M（约 0.5–1 天）。

### 2.3 补 ai-chat 单测
- **现状/风险**：`ai-chat` 已留 `_test` 钩子，但无测试文件/runner；合规逻辑（接地/敏感词/JSON 解析）值得固化。
- **修改建议**：加 `cloudfunctions/ai-chat/test/ai-chat.test.js`（Node 原生 `node:test`），覆盖：敏感词命中拦截、年份矛盾拦截（`lexicalUngrounded`）、JSON 解析降级、无上下文兜底。CI 跑。
- **涉及文件**：`cloudfunctions/ai-chat/test/`（新建）、`package.json`（加 `test` script）。
- **依赖**：1.2 根 package.json 后更易跑。
- **验证**：`npm test` 全绿。
- **工作量**：M（约 0.5 天）。

---

## Phase 3 — 安全头与细节（长期·按需）

### 3.1 CSP 响应头（★最后做）
- **现状/风险**：静态托管无 `Content-Security-Policy` 等安全响应头；内联 `<style>` 大量存在，若直接禁用 `unsafe-inline` 会破坏样式。
- **修改建议**：
  1. **先**将关键内联 `<style>` 抽到 `style.css`/`nav-embed.css`（消除 `unsafe-inline` 依赖）。
  2. **后**在自定义域名 + 边缘函数返回 `Content-Security-Policy`（img/src 限本站 + wikimedia + data:；script 限本站 + cloudbase CDN；禁用 `unsafe-inline`/`eval`）。
- **涉及文件**：各 HTML 内联 `<style>`、`style.css`、新增边缘函数。
- **依赖**：自定义域名已绑（见 0.2）；内联 style 已外移（本项步骤 1）。
- **验证**：浏览器 DevTools 看响应头含 CSP；页面无样式丢失、无违规资源被拦。
- **工作量**：L（约 1–2 天，散布多页）。

### 3.2 authMode 命名修正
- **现状/风险**：`RCS.config.authMode="email"` 与实际手机号主导流程语义不符（非 bug，易误读）。
- **修改建议**：改名 `phone` 或加注释说明"虽名 email，实际走手机号登录"。
- **涉及文件**：`cloudbase-config.js`、`auth-service.js` 相关引用。
- **依赖**：无。
- **验证**：搜索 `authMode` 语义一致。
- **工作量**：S。

### 3.3 视觉微调（主观可选）
- 页脚升级为 Apple 式多列 mega-footer；时间轴/留白可再精修。**非缺陷，按审美取舍。**

---

## 工作量汇总

| 编号 | 事项 | 优先级 | 工作量 | 阶段 |
|---|---|---|---|---|
| 0.1 | 生成器禁用注释/移除 | P1·灭火 | S | Phase 0 |
| 0.2 | 备案续费启动 | P1·硬截止 | M(人工) | Phase 0 |
| 0.3 | 排行榜挂页面/软屏蔽 | P1·审计遗留 | M | Phase 0 |
| 1.1 | 统一脚本加载路径 | P1·结构 | M | Phase 1 |
| 1.2 | 根 package.json + 脚本 | P2 | M | Phase 1 |
| 1.3 | 敏感词库合并 | P2 | S | Phase 1 |
| 2.1 | 自托管字体 | P2·性能 | M | Phase 2 |
| 2.2 | corpus 分片 | P3·性能 | M | Phase 2 |
| 2.3 | ai-chat 单测 | P3 | M | Phase 2 |
| 3.1 | CSP 头 | P3·安全 | L | Phase 3 |
| 3.2 | authMode 改名 | P3 | S | Phase 3 |
| 3.3 | 视觉微调 | P3·可选 | S | Phase 3 |

> 工作量：S≈15min–1h，M≈0.5–1d，L≈1–2d。Phase 0 三项可立即并行启动；Phase 1 建议按 1.1→1.2→1.3 顺序；Phase 3 的 3.1 必须最后。

---

## 一句话执行纪律
**先拆弹（0.1）→ 保环境（0.2）→ 闭环孤儿（0.3）→ 收口双路径（1.1）→ 建编排（1.2）→ 去重复（1.3）→ 提性能（2.x）→ 补安全头（3.1）。每一步改完都用真机在 45 页 + 7 红文页各验一次登录态/AI 角标。**
